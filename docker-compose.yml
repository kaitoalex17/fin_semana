version: '3.8'

services:
  fin_semana:
    build: .
    container_name: fin_semana_status
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
